### Semantic Summary

#### Key Functionality
The `FileProcessResult` class is designed to encapsulate the results of a file processing operation, particularly when executing external commands or scripts. It captures various aspects of the command execution such as standard output, standard error, exit value, and timing details.

#### Core Logic
The core logic involves storing and retrieving information about the outcome of a file processing task. This includes handling the standard output (`stdout`), standard error (`stderr`), exit status of the process, time taken to execute the process, and whether any output was truncated due to length constraints.

#### Inputs/Outputs
- **Inputs**: The class does not directly accept inputs but rather stores data that can be set through setter methods. These include strings for `stdout` and `stderr`, an integer for `exitValue`, and longs for `processTimeMillis`, `stdoutLength`, and `stderrLength`.
- **Outputs**: The class provides getter methods to retrieve the stored data. Additionally, it overrides the `toString()` method to provide a formatted string representation of the object's state.

#### Dependencies
- **Dependencies**: The class does not have explicit dependencies on other classes within the provided snippet. However, it is part of the `org.apache.tika.utils` package, suggesting it might depend on other classes or utilities within the Tika library. There are no direct imports or references to other classes in the given code.

In summary, the `FileProcessResult` class serves as a container for the results of a file processing operation, providing a structured way to store and access detailed information about the process execution.